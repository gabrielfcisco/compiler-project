# 🧠 Máquina Virtual Didática (MVD) com Interface Gráfica em Python

Este projeto implementa a **Máquina Virtual Didática (MVD)** usada na disciplina de **Construção de Compiladores**.  
A MVD executa programas compilados da linguagem **LPD** (arquivos `.obj`) e apresenta uma **interface gráfica (GUI)** desenvolvida em **Python (Tkinter)** para acompanhar a execução das instruções, a pilha de memória e as saídas do programa.

---

## ⚙️ **Como Executar o Projeto**

Para iniciar a Máquina Virtual Didática com interface gráfica, basta executar o comando abaixo no terminal (na pasta do projeto):

```bash
python mvd_gui.py
